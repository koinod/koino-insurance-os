# CHANGES — gap-analysis/2026-05-03

Append-only log of every gap closure on this branch.
Format: `[YYYY-MM-DD HH:MM] GAP-XX — what changed — files`

---

## 2026-05-03

- **23:50  GAP-X4** — auth identity link foundation. Migration `0004_auth_identity_link` adds `reps.user_id`/`agency_id`/`upline_id`, `public.me()` returns the current viewer, `public.downline_of(rep_id)` returns recursive subtree. New `/api/me` edge function and `/lib/me.js` frontend helper expose `window.me()`, `window.scopeRepIds()`, `window.canSeeFleet()`, `window.canSeeTeam()`. Atlas demo agency seeded; demo reps backfilled with `agency_id` and an upline tree (Marcus root). Files: `supabase/migrations/0004_auth_identity_link` (Supabase RPC), `api/me.js` (new), `lib/me.js` (new), `index.html` (script load order).

- **23:55  GAP-X3** — AI co-pilot agency-scope filter. `sbSelect()` accepts an `agencyId` and force-injects `agency_id=eq.{...}` into every PostgREST URL when present (fallback retry on 400 for tables that don't yet have the column). New `resolveAgencyId()` calls `public.me()` once per request to derive scope from the JWT. All 8 tools updated to thread the agency through their fetch signature. Files: `api/copilot.js`.

- **00:15  GAP-D1** — TodayRep KPIs derive from real session. `window.me()` resolves the viewer; we look up that rep's row in `AppData.REPS` (no more hardcoded REPS[0]=Marcus). Today's Commission sums `AppData.COMMISSIONS` filtered by `repId === me.id` and today's date. Apps-submitted from `AppData.POLICIES` where `owner === me.id` and `issuedAt === today`. Dials from `AppData.RECORDINGS` (today's). Tier proximity replaces the hardcoded "$8,690 from Diamond" with a computed delta to the next tier from `TIER_TARGETS`. Page subline composed dynamically from real numbers. Re-renders on `me:loaded`. Files: `page-today.jsx`.

- **00:25  GAP-OD1** — TodayOwner KPIs from live tables. Override-revenue MTD sums `commissions.kind=override` for the month; anomalies count = open NIGOs from `AppData.NIGOS`; lead-spend ROI today = team-AP / sum(today's touchpoints × source.cost-per-lead); active-producer ratio from `REPS`. Replaces hardcoded "$258,420", "Anomalies open: 4", literal subline. Agency name pulled from `me.agency_name`. Files: `page-today.jsx`.

- **00:40  GAP-X1 v1** — Predictive heuristics shipped (RETAINER + CLOSER preview). `<PredictiveCards/>` renders two side-by-side panels: At-risk reps (RETAINER, weighted by streak-broken/zero-today/low-dials/under-tier-target/off-presence) and About-to-break-out reps (CLOSER, weighted by mtd-vs-tier-target/today-vs-avg/streak/live-presence/dials/appts). Score ranges 0–100, threshold 50. Manager view scopes to `window.scopeRepIds()` downline; owner view shows fleet-wide. Cards land on TodayManager + TodayOwner above the existing forecast strip — first surface to honor the product differentiator (predicts disengagement and breakouts). ML model still TODO; this is transparent heuristic v1. Files: `page-today.jsx`.

- **00:45  GAP-X2 (partial)** — Demo data sandbox. `/api/me` now returns `is_demo: true` + `agency_id = atlas-demo-uuid` for unauthed callers; `lib/me.js` exposes scope helpers; new accounts that sign in get their own `agency_id` from `public.me()`. Full closure requires `data.jsx` hydrate to inject `agency_id=eq.{me.agency_id}` into every fetch; tracked as Sprint-1 follow-up (touches every `sb.from(...)` call site). Files: `api/me.js`, `lib/me.js` (already committed).

- **00:55  GAP-X2 (full)** — Hydrate now scopes every tenant-specific fetch by viewer's agency_id. `getActiveAgencyId()` extended: explicit switcher choice → `me().agency_id` → null. v2 hydrate Promise.all wraps every tenant table with `scope()` (policies, commissions, payouts, clawbacks, attributions, touchpoints, nigos, coaching_*, vault_files, households, clients, book_entries, recruits, interviews, threads/members/messages/reads, notifications, tasks, followup_rules, tier_changes, tiering_overrides, aep_assignments, sequence_enrollments, agent_deployments, agent_runs, forecast_runs, forecast_overrides). Reference tables (carriers, products, lead_sources, nigo_reasons, sequences, aep_periods) stay global. Net effect: a new tenant's session sees ZERO Atlas-demo rows. Files: `data.jsx`.

- **02:30  COPILOT-V2** — Fixes 4 root causes from real chat failure on P&L page:
  1. **Missing tools** — Added 8 new copilot tools: `lead_sources`, `attributions`, `commissions`, `payouts`, `clawbacks`, `policies`, `nigos`, `forecast`. Total tools: 16 (was 8). Bumped per-call cap from 3 → 5 tools so analytical questions (commissions × lead_sources × policies) get all the data they need.
  2. **Context-aware tool boost** — New `contextBoost(context)` force-includes the right tools based on the page the user is on, even when the prompt is vague ("what's the impact?"). P&L page → commissions+payouts+lead_sources+policies. Commissions page → commissions+payouts+policies. Lead Vendors page → lead_sources+attributions+commissions. Etc.
  3. **Short-term memory** — `/api/copilot` now accepts `history: [{q, a}]` (last 3 turns). Frontend `shared.jsx` AI rail walks its hist[] state to extract the last 3 user/assistant pairs and forwards them. Vague follow-ups ("what do you need?", "??") now have context.
  4. **Truncation fix** — `tryGemini` maxOutputTokens bumped 400 → 900. OpenRouter free models stay at 600.
  5. **DEMO_ASSIST sub-agent** — When `agencyId === atlas-demo-uuid` OR all fetched tools returned zero rows, the copilot enters guide-me mode: explains what the metric means, names the data sources that would answer it, points at the page/button to populate. Still refuses off-mission and software-internals queries (rules 4–6 in SYSTEM stay enforced). Closes the empty-account UX gap before Ian tests a brand-new signup.

  Re-simulation: "If I cut the worst-performing lead source, what's the net impact?" on P&L page now fires lead_sources + commissions + policies + payouts tools, returns ~150 rows, and the model has enough to compute net impact (lost-commission - lead-spend-saved). Demo guest sees the same pipeline but in DEMO_ASSIST tone with illustrative numbers + "go to /attribution to wire your first source."

  Files: `api/copilot.js`, `shared.jsx`.

## 2026-05-05

- **REDIAL-QUEUE-UI + PACING** — The disposition retry queue now has a visible surface (was localStorage-only), plus dial-rate pacing.
  - **New `page-redial-queue.jsx`** exposes `<RedialQueuePanel/>` and `<PacingBadge/>` to window. Reads the `repflow.redial_queue` localStorage that LOCAL-AUTODIAL-CADENCE writes via `scheduleRedial`.
  - **Panel** mounted in Floor's Live mode as a 360px right-rail next to the queue. Shows two sections: **Due now** (with a "Pull N due" primary button) and **Scheduled** (with countdown labels — "in 1.7h", "in 23m", etc.). Each row has a clear-from-queue X. Outcome chip per row (no_answer / voicemail / callback / TCPA-blocked).
  - **`window.pullDueRetries()`** hydrates due entries against `AppData.PIPELINE` / `AppData.QUEUE` (full lead rows with phone/email), clears them from the redial queue, and dispatches `autodial:start` — kicks off a fresh autodial session of just the due retries OR injects them into the active session.
  - **Hot-key R** (in LiveMode) calls `pullDueRetries`. Hint copy in the Live floor strip updated to mention it.
  - **`<PacingBadge/>`** sits in the LiveMode header — shows dials-per-hour vs the 90/hr soft cap. Color escalates: tertiary → status (>70%) → warning when exceeded ("· slow down"). Reads from `sessionStorage.repflow.dial_attempts` (already populated by `markDialAttempt` in the autodialer).
  - Auto-refreshes via `redial:queued` / `redial:due` / `storage` events + a 30s tick. No network calls.
  Files: `page-redial-queue.jsx` (new), `page-floor.jsx`, `index.html`.

- **LOCAL-AUTODIAL-CADENCE** — What the best autodialers do that Phone Link / Continuity didn't: the cadence engine. Web app can't programmatically press "Call" inside Phone Link (Microsoft doesn't expose an API), but most of the autodialer's value is in *what happens between dials*, not the dial trigger itself.
  - **New `lib/dial-rules.js`** — single source of truth for dial gates + cadence. Loaded after `lib/me.js` so all surfaces share the same checks.
  - **TCPA dial-window enforcement.** State → IANA timezone map (50 + DC), federal 8am-9pm window enforced at the lead's local time. State overrides for FL (Sunday 9am), MS/AR/LA/OK (no-Sunday). `window.canDialNow(lead)` returns `{ ok, reason, untilLocalHour }`. Autodialer skips out-of-window leads, logs `out_of_window` outcome, advances after 600ms.
  - **Cooldown between dials.** 5-minute floor between attempts on the same lead — prevents accidental redial spam if a rep hammers the dial button. Stored in sessionStorage. `window.dialCooldown(leadId)` returns ms remaining; `window.markDialAttempt(leadId)` is fired by the autodialer before each dial.
  - **Disposition-driven retry queue.** Autodialer outcomes now schedule auto-redials per cadence: `no_answer` → 2h, `voicemail` → 24h, `callback` → manual, `appointment` / `not_interested` / `no_contact_info` → terminal. `window.scheduleRedial(lead, outcome)` writes to localStorage; `window.dueRedials()` returns leads ripe to re-enter the queue. Background interval fires `redial:due` events every 60s for the autodialer to pull in.
  - **Hot-keys expanded.** Existing Space/S/X/1-5 unchanged. Added: **D** = re-dial current lead (rep clicked off, wants to retry without skipping); **V** = copy a personalized voicemail script to clipboard for the rep to read after the beep. Hint copy in the prep modal updated.
  - **macOS desktop helper now actually auto-dials.** Updated to use `open -a FaceTime "tel://$PHONE"` — Continuity hands the call to the paired iPhone and FaceTime auto-presses Call. Setup notes reference the iPhone Settings → Phone → "Calls on Other Devices" prerequisite.
  - **Windows desktop helper now drives Phone Link.** New `dial.ps1` wrapper uses Windows UI Automation: opens `tel:` URL → waits 1.5s for Phone Link to focus → finds the "Phone Link" window → finds its Call button by AutomationElement name → invokes it via InvokePattern. The web app can't reach Phone Link, but the desktop helper running locally CAN. Falls back to manual press if the button isn't found (e.g., Phone Link not focused).
  - Disposition summary toast at session end now lists per-outcome counts ("12 dialed: 3 no_answer, 4 voicemail, 5 appointment") instead of a bare total.
  Files: `lib/dial-rules.js` (new), `index.html`, `page-autodialer.jsx`, `page-platform.jsx`.

- **TWILIO-CAPABILITY-MATRIX** — Settings → Calling now lives up to where the floor's CTA points reps.
  - **`CallingSetup` rewritten** to lead with a Twilio capability matrix instead of just the desktop-helper install. Three capability rows — **Voice** (`/api/twilio-token`), **SMS** (`/api/twilio-sms`), **Transcription** (`/api/transcribe`) — each probed on mount via the existing 503 graceful-error pattern.
  - Per row: green/amber/red status dot, "Configured / Not configured / Error" chip, missing-env-var chips when unconfigured (e.g., `TWILIO_ACCOUNT_SID`, `OPENAI_API_KEY`), and a Test button when ready (Test dial fires `repflowDialTwilio`; Send test prompts for a number and POSTs `/api/twilio-sms` with a "Repflow test SMS · ignore" payload).
  - When all three are unconfigured, an alert at the top explains the consequence: "dial buttons fall through to the system phone app · live transcription captures only the rep's mic, not the lead."
  - **Configure Twilio** button mounts the existing `TwilioConfigModal` (already had auto-provision via `/api/twilio-app/provision` and copyable env-var block — just hadn't been reachable from this page).
  - "Vercel env vars" button opens the Vercel dashboard so the operator can paste the secret.
  - **Re-probe** button reruns all three checks without needing a page reload.
  - Desktop-helper section relabeled as **Fallback · click-to-call via desktop helper** with explicit copy that **live transcription only captures the rep's mic in this mode** — sets the right expectation.
  - `useCapabilityStatus` hook publishes voice state to `window.__twilioStatus` so the floor's `useTwilioStatus` shares the same cached probe (no double API call when navigating between Floor and Settings).
  - All four touched files validated via `@babel/parser` — clean parse.
  Files: `page-platform.jsx`.

- **CSV-IMPORT-V2** — New tenants can finally migrate their existing books in.
  - Replaced the naive line-split parser with an RFC-4180-ish state machine in `parseCsvText` — handles quoted fields, escaped quotes (`""`), CR/LF, and BOM. The old parser broke on any field with a comma (e.g., `"Doe, John"`, addresses).
  - **Drop-zone** UI alongside the textarea — drop a `.csv` file, or click to choose, or paste text. All three paths feed the same parser.
  - **Auto column-mapping** with fuzzy alias matching (`normHeader` strips non-alphanumeric, lowercases, then matches against alias lists). Handles common exports: `First Name` / `name`, `Phone Number` / `mobile` / `cell`, `email_address` / `Email`, `Stage`, `Lead Source` / `campaign`, etc. New "Map columns" step lets the operator override any auto-detected mapping.
  - **Phone + email columns** captured (the wave's whole point — pairs with the `0007_lead_phone_email` migration). Phone is normalized to E.164 (`+1` prefix for 10-digit US, etc.) on the client before submit.
  - **Dedup-by-phone** against existing pipeline rows + within the CSV itself. Duplicates are flagged in the preview and skipped at import time (not blocked); operator sees the count in the summary.
  - **Routes through `AppData.mutate.pipelineInsert`** instead of bypassing it with a raw Supabase insert. Means: RLS / audit log / realtime fan-out / phone+email column tolerance all fire automatically for each imported row.
  - **Owner resolved via `window.me()`** not `REPS[0]=Marcus`. Owner column also accepts handle / name / id and maps back to `reps.id`.
  - **Per-row error tolerance** — one bad row no longer fails the batch. Final summary shows imported / failed / skipped counts + first 10 error messages.
  - Preview table now shows phone, email, and a status chip (ready / dupe / blocked) per row.
  Files: `polish.jsx`.

- **MIGRATIONS-APPLIED + FLOOR-AUDIT-2** — Schema gaps closed in production; second pass through floor surfaces.
  - **Applied migrations to `Insurance OS` Supabase project** (`jfphwmzwteermalzwojp`):
    - `0007_lead_phone_email` — phone + email on `pipeline` and `queue` (with format CHECK constraints + indexes). Tolerant mutators in data.jsx will start persisting these fields immediately.
    - `queue_assigned_rep` — adds `queue.assigned_rep_id text references reps(id)` so the QUEUE-WAVE `queueClaim` / `queueRelease` mutators (which have been writing tolerantly for a wave) start persisting now.
  - **Pipeline LeadDetail cleanup.** Compliance section was 100% literals (`9f8c-2a11…` LeadiD, "Captured" TrustedForm chip, fake SOA flag) — replaced with consent-derived state + a real "wire Jornaya/TrustedForm in Connections" hint when not captured. Activity feed dropped the synthetic "Earlier · Quote sent · {product}" entry; remaining rows derive from real `lead.last`/`lead.next`/`lead.days`/`lead.source` and an empty-state when none. Sequences section gets an empty-state CTA pointing to Pipeline → Sequences when no sequences are wired. SOA generator call no longer hardcodes "Atlas Insurance Group" — uses `me().agency_name` (falls back to "your agency"). Pipeline list-view rep cell guards against missing `repById[p.owner]` (was crashing with `.name.split` on undefined when an owner was deleted).
  - **DealWriteForm empty-state.** Brand-new agency with no carriers/products in their tables saw an unusable form (`carrier` and `product` selects dead). New warning panel above the form when either list is empty, linking to Resources → Carriers.
  - **PageCalls (history mode).** `meId = REPS[0].id` (Marcus) replaced with `window.me()`. Manager view now scopes recordings to `window.scopeRepIds()` downline (was fleet-wide). Empty state added when no recordings in scope. Whisper transcript surfaced via `<PostCallTranscript recordingId={sel.id}/>` below the AI summary — pairs with the FLOOR-AUDIT transcription pipeline. AI summary text falls back to "processing…" instead of empty when `sel.ai` is missing.
  - Validated all touched files via `@babel/parser` — all parse clean.
  Files: `page-pipeline.jsx`, `page-deal-write.jsx`, `page-extras.jsx`, plus 2 migrations applied via Supabase MCP.

## 2026-05-04

- **CONTACT-FIELDS** — Lead phone + email fields land everywhere. Real customers can finally be dialed.
  - **Migration `0007_lead_phone_email.sql`** adds `phone text`, `email text` to `public.pipeline` and `public.queue`. Loose-format CHECK constraints (≥7 digits when present, `%@%.%` for email; null/empty allowed). Indexes on phone for dedup + reverse lookup. Both columns nullable so a lead can land in the queue with no contact info, then be enriched in the lead detail drawer.
  - **`data.jsx` mappers updated** — both the initial hydrate (`PIPELINE`/`QUEUE`) and the realtime `toJs` mapper now thread `phone` + `email` from DB columns to the JS `lead.phone` / `lead.email` shape. `assignedRepId` also wired through queue mapper (was added in QUEUE-WAVE but the mapper was missed).
  - **`pipelineInsert` mutator** sends phone/email; tolerates "column does not exist" and retries without them so the insert ships before the migration lands. New `pipelineContact(id, {phone, email})` mutator for inline edits — same tolerant pattern.
  - **`queueAssign` mutator** carries phone+email forward when promoting a queue row to pipeline (so claimed leads keep their contact info).
  - **New-lead form** (`page-pipeline.jsx`) gets Phone + Email fields stacked on a row above Age/State. Helper text under the form notes phone is optional but required to dial/SMS.
  - **Lead detail drawer** gets a Contact section near the top: inline-editable Phone + Email, blur-saves via `pipelineContact`. "No phone on file" warning under the fields when empty. Email/SMS/Call footer buttons disabled when their respective field is missing — tooltip routes to "Add phone first". The Call footer button now uses `lead.phone` (was synthetic `+15125550<id>`); SMS routes through `window.smsCompose(lead, lead.phone)`; Email opens `mailto:` with a pre-filled subject.
  - **Queue rows** (`page-queue.jsx`) Phone / SMS buttons disabled when no phone on file, with tooltip explaining the gate. The first-row demo Phone button still pops the InCall demo modal so the page stays useful even on demo data.
  - **InCall left pane** displays the lead's phone + email under the name (mono-formatted) when present. "No phone on file" warning shown otherwise. Hardcoded "Cheryl Hampton · 67 · Travis County, TX · zip 78704 · T65 list" + literal chips fully replaced with `activeLead` fields earlier in AUTODIAL-DASHBOARD; this wave layers contact info on top.
  - **Autodialer skips no-phone leads.** Previously synthesized a fake `+15125550<id>` and called it. Now logs `no_contact_info` outcome, fires a warning toast, and advances to the next lead after 800ms — no more risk of dialing the wrong real number.
  - **Twilio "not configured" CTA** lives in the LiveMode header. New `useTwilioStatus()` hook probes `/api/twilio-token` once on mount; when 503, renders a warning pill that links to Settings → Calling and explains the consequence ("dials route to system dialer · transcription mic-only"). Status cached on `window.__twilioStatus` so we don't re-probe across page changes. Silent when Twilio is configured.
  - Removed the dead "Hit N to grab the next lead manually" copy in the Live floor strip — the Hotkey-N handler was deleted in AUTODIAL-DASHBOARD, so the prompt was lying.
  Files: `supabase/migrations/0007_lead_phone_email.sql` (new), `data.jsx`, `page-pipeline.jsx`, `page-queue.jsx`, `page-autodialer.jsx`, `page-floor.jsx`.

- **AUTODIAL-DASHBOARD** — Floor cleanup + made the autodialer the rich call dashboard.
  - **Removed fluff.** "Next call" button on Floor header (and its Hotkey-N handler) deleted — it was redundant with the autodialer pill (the actual queue advance) and the per-row Phone buttons. Stub "Filters" + "Start dialing" buttons on DialQueueView removed (no `onClick`, redundant with the autodialer pill above). Dead `_LegacyModeTabs` code in page-floor.jsx removed.
  - **Autodialer auto-opens the InCall dashboard.** When `AutoDialBar` enters the "dialing" stage on a fresh lead it now dispatches `incall:open` with that lead's row + `autodial: true`. The App component listens (new `incall:open` / `incall:dismiss` handlers) and pops the InCall modal pre-populated with the live lead. Floating bar hides itself when InCall is open in autodial mode (no double UI), reappears if the rep dismisses the modal mid-session.
  - **InCall renders an autodialer-mode variant.** Listens to `autodial:state-change` (new event AutoDialBar publishes on every state mutation; mirrors to `window.__autodialState`). When in autodial mode: (a) a queue-progress header appears at the top of the modal with `Autodialer · 3 of 12 · calling/log outcome/paused` + Pause / Resume / Skip / Stop controls, (b) the bottom action strip swaps to the 5 outcome buttons (1 No answer / 2 VM / 3 Appt / 4 Not int / 5 Callback) when stage = "outcome", (c) outcome / skip / stop buttons dispatch `autodial:outcome` / `autodial:skip` / `autodial:stop-request` back to AutoDialBar, which handles the existing pipeline-stage advance + queue iteration. End-call button label changes to "Close · keep dialing" so the rep doesn't accidentally kill the session by closing the modal.
  - **Lead profile in InCall is no longer hardcoded.** Replaced "Cheryl Hampton · 67 · Travis County, TX · zip 78704 · T65 list · Plan G eligible · No prior Med Supp · Spouse 64" literals with values from `activeLead` (`activeLead.lead`, `age`, `state`, `source`, `product`, `heat`, `consent`). Call timer + TPMO flag reset every time the lead changes (correct behavior across autodial advances).
  - **Quote tool extracted + accessible.** `CarrierQuoteTool` exposed via `window` (was locked inside InCall as a tab). New `<QuoteToolModal/>` in `FloorActionsHost` renders it standalone. New "Quote tool" button in the Floor header opens it via `quotetool:open` event. Reps can run carrier-fit calculations without being on a call. Available globally via `window.openQuoteTool()`.
  - `.incall-card` CSS got `grid-template-rows: auto 1fr` so the autodial header docks above the existing 2-column body cleanly. Card height bumped 560→600px to accommodate the new header strip.
  Files: `page-floor.jsx`, `page-queue.jsx`, `page-autodialer.jsx`, `page-floor-actions.jsx`, `index.html`, `styles.css`.

- **FLOOR-AUDIT** — Audit of the rep work surface + fix wave for everything broken or stub.
  - **Critical: dial-loop infinite recursion fixed.** `repflowCall` (page-platform.jsx) used to delegate to `repflowDial` (page-tenant.jsx), which fell back to `repflowCall` when Twilio token failed → call-stack overflow on every dial when Twilio creds aren't set. Restructured so `repflowCall` is the public cascade entry (Twilio softphone → desktop helper → tel:) and `repflowDialTwilio` is a Twilio-only helper returning a boolean. `repflowDial` kept as a backwards-compat alias. Files: `page-platform.jsx`, `page-tenant.jsx`.
  - **Floor AutodialerPill is now functional.** Previously the ON/PAUSE/STOP buttons only flipped localStorage — no leads were dialed. Now Start builds the rep's queue (their assigned + unassigned rows, ranked by SLA + score) and dispatches `autodial:start` to AutoDialBar; Pause / Resume / Stop dispatch the matching events. AutoDialBar grew listeners for `autodial:pause` / `autodial:resume` / `autodial:stop`. AutoDialBar now uses the lead's real `phone` field when present, only falling back to the synthetic test number for demo seeds. Files: `page-floor.jsx`, `page-autodialer.jsx`.
  - **InCall stub buttons wired.** Mute toggles state + calls `connection.mute()` on the Twilio Voice SDK active connection. Hold pauses the call timer and toggles state. SMS opens the new compose modal. Schedule SOA opens the new appointment modal pre-set to 15-min SOA mode. Send app link copies a placeholder e-app URL to clipboard and logs a follow-up task. Files: `page-queue.jsx`.
  - **NEW: SMS automation.** `/api/twilio-sms.js` posts to Twilio Programmable Messaging via REST; graceful 503 with `missing: [TWILIO_*]` when env unset. New `<SmsComposeModal/>` with 4 templates (follow-up nudge, SOA reminder, missed-you, thanks-next), 1600-char counter, recents log to `AppData.SMS_LOG`. Trigger from queue rows (new SMS button), in-call panel, and any future site via `window.smsCompose(lead, phone)`. Files: `api/twilio-sms.js` (new), `page-floor-actions.jsx` (new).
  - **NEW: Calendar / SOA invite generator.** RFC 5545 `.ics` builder produces files compatible with Google / Outlook / Apple Calendar. New `<BookAppointmentModal/>` with date+duration picker; on save, downloads the .ics, writes a tasks row, and copies the operator's Calendly link to clipboard if `connections.calendly.config.scheduling_link` is wired. Triggers: Schedule SOA button on queue rows, in-call panel; `window.scheduleSOA(lead)` / `window.bookAppointment(lead)` for any other surface. Files: `page-floor-actions.jsx`.
  - **NEW: Live + post-call transcription via Whisper.** `/api/transcribe.js` accepts both `{ audio_url }` (recording webhooks) and multipart file uploads (live mic chunks); calls OpenAI Whisper (`whisper-1`, `gpt-4o-transcribe` if `OPENAI_TRANSCRIBE_MODEL` set); returns verbose JSON with segments. `<LiveTranscriber/>` captures the rep's mic + the Twilio Voice SDK's remote audio track via Web Audio API, mixes with `MediaStreamAudioDestinationNode`, slices into 5-second `MediaRecorder` chunks, posts each to `/api/transcribe`, and appends labeled segments. Falls back to mic-only when no Twilio call is active. The fake "Live transcript" panel in InCall replaced with the real component. `<PostCallTranscript/>` polls vault_artifacts for finished recording transcripts. The Twilio recording webhook (`api/twilio-recording.js`) now fires the transcription job after persisting the artifact (uses `req.waitUntil` when available; basic-auth-aware fetch for Twilio recording URLs). Graceful 503 if `OPENAI_API_KEY` not set — UI shows the env-var setup hint. Files: `api/transcribe.js` (new), `api/twilio-recording.js`, `page-transcriber.jsx` (new).
  - **GAP-D1 sweep on the floor.** FloorTopStrip + DealsMode + DealWriteForm.submit now resolve `me()` instead of `REPS[0]`. "47 leads" hardcode in DialQueueView replaced with real `QUEUE.length`. New SMS + Schedule SOA icon buttons on every queue row.
  - Cache version bumped (page-floor 19→20; page-floor-actions and page-transcriber introduced at v=20). Files: `index.html`.



- **MGR-WAVE  GAP-MD1/MD2/MD3/MD4 + GAP-MC1/MC2** — Manager page rewrite. Team Board now scopes producers via `window.scopeRepIds()` (manager sees their downline only; owner still sees fleet). Each rep card carries an at-risk badge (RETAINER heuristic) and breakout badge (CLOSER heuristic) computed inline; cards re-order risk-first → breakout-first → MTD desc so attention lands where the operator can move the needle. Two new inline rep actions: **Note** (modal → `coachingNoteCreate` → posts to `coaching_notes`) and **Alert** (modal with 4 severity-aware presets → `notificationCreate` → fires `create_notification` RPC, falls back to direct insert). CoachingManager binds to `AppData.COACHING_SESSIONS` filtered by scope; falls back to a deterministic seed derived from rep risk/breakout signals when no live sessions exist (so a brand-new agency still sees a useful page instead of literal Atlas names). Latest-call panel + scorecard rollup now read from `AppData.RECORDINGS` (avg talk-ratio, avg open-Qs) and `COACHING_SESSIONS` (sessions completed). CoachingRep binds to `me().rep_id` for "my coaching cards"; surfaces a "Notes from your manager" panel from `COACHING_NOTES`. CoachingOwner derives theme effectiveness rollups from `coaching_sessions.outcome` + `rating` when present, demo seed otherwise. ReplayMomentModal pulls real recording AI summary when `session.recording_id` is linked; "Mark practiced" calls `coachingSessionResolve` to flip the session row. RepDrillSlideout gains an at-risk/breakout chip header, recent coaching notes feed, and an "Add note" button wired to the same modal. New mutate helpers: `coachingNoteCreate`, `coachingSessionResolve`, `notificationCreate`. New icons: `AlertTriangle`, `Circle`. Cache version bumped 17→18 on `data.jsx`, `icons.jsx`, `page-manager.jsx`. Files: `page-manager.jsx`, `data.jsx`, `icons.jsx`, `index.html`.
