# Handoff Prompt — 0-to-100 Execution

Paste this into a fresh Claude Code session. It is self-contained.

---

You are picking up RepFlow's production-readiness sprint on `/Users/macmini/repos/koino-insurance-os`. The previous session ran 5 parallel agents in worktrees and produced 18 commits across 5 `sprint/*` branches but did not merge them. Ian has also been running parallel work that introduced 4 more `sprint/*` branches.

**Your job:** Take the codebase from current state to production-ready in this single session. Specifically: merge the pending branches safely, apply the migrations Ian approves, validate prod, then build the recruiting funnel applicant-intake + funnel-page integration. Per `~/CLAUDE.md` the recruiting funnel is the Rookie of the Year Play and is the actual mission.

**Read these files in order before touching anything:**
1. `~/CLAUDE.md` — mission, operator preferences (Hormozi-direct, terse, decide-don't-ask).
2. `~/.claude/rules/anti-hallucination.md` — every metric must be backed by a file; failed tool call ≠ success.
3. `audits/SPRINT_RETRO_2026-05-15.md` — branch inventory, critical findings, merge order.
4. `audits/SPRINT_RETRO_2026-05-16.md` — this session's context.
5. `audits/RLS_AUDIT.md` (on `sprint/audits-bundle` branch; check it out to read) — the cross-tenant leak you'll be patching.
6. `audits/SCHEMA_DRIFT.md` (same branch) — the 13 missing tables, 15 columns.
7. `audits/ONBOARDING_GAPS.md` (on `sprint/onboarding`) — the 5 missing RPCs.

**Inventory commands** (run these first, report back before doing anything):
```
cd ~/repos/koino-insurance-os
git fetch origin
git status -sb
git log origin/main --oneline -20
git branch -a | grep -E "sprint/|worktree-agent-"
ls -la .claude/worktrees/
ls supabase/migrations/ | tail -20
```

---

## Phase A — Merge 9 sprint branches (90-120 min)

### 🚨 Stale-branch trap (READ THIS FIRST)
Ian's 4 branches (`sprint/leaddrip-phase2-sender`, `sprint/smoke-interactions`, `sprint/tcpa-audit`, `sprint/twilio-inbound`) were forked from a main that did NOT yet have migrations `0037_single_tenancy_reps_managers.sql`, `0038_vault_starter_autoseed_new_agency.sql`, `0039_vault_storage_bucket.sql`, `0040_security_advisor_inline.sql`, nor `tests/vault-flows.mjs`. A naive merge will DELETE all of those. **You MUST rebase each of Ian's 4 branches onto current main before merging.** Verify the rebased diff doesn't show those deletions before proceeding.

`sprint/pre-flight` is already on main (commit `256d549`). Skip it.

### Order (dependencies require this sequence)
1. **`sprint/audits-bundle`** — schema-drift commit must merge BEFORE the RLS-harden commit on the same branch; RLS references columns added by drift. Verify commit order within the branch before merging.
2. **`sprint/api-hardening`** — security, standalone.
3. **`sprint/code-quality`** — broad `.jsx` sweep (91 silent catches fixed, toast taxonomy normalized). Merge before scaffolds + Ian's branches to minimize conflict surface.
4. **`sprint/tcpa-audit`** (rebase first) — TCPA compliance gates. Pre-req for any SMS outreach in prod.
5. **`sprint/twilio-inbound`** (rebase first) — STOP/START opt-out handling. TCPA-required complement to outbound.
6. **`sprint/leaddrip-phase2-sender`** (rebase first) — Twilio sender with compliance gates. Builds on the two above.
7. **`sprint/smoke-interactions`** (rebase first) — click-driven smoke suite. Merge after the SMS branches so the suite covers them.
8. **`sprint/onboarding`** — wizard trap-loop fixes. Doesn't conflict with SMS work.
9. **`sprint/scaffolds`** — recruits table + autodial→Supabase + Playwright flows.

### Decision already made: page-recruiting.jsx vs page-recruits.jsx
Ian called sovereign-execution on this. The call: **keep `page-recruiting.jsx` as the canonical Recruiting route. Migrate the kanban content from `page-recruits.jsx` into a new "Funnel" tab inside `page-recruiting.jsx`, make Funnel the default landing tab, then DELETE the standalone `page-recruits.jsx` + its app.jsx route + its NAV entry.** Rationale: one canonical route per function (per the no-clutter feedback memory), funnel-first is what the operator asked for ("main page should be focused on leads dripping in" applied to recruits = funnel-first), workbench tabs (Campaigns / Messages / Applicants) stay as adjacent tabs. Do this rewrite during the `sprint/scaffolds` merge — don't punt it.

### Per-merge protocol (non-negotiable)
1. If branch is from Ian's 4: `git checkout sprint/<name> && git rebase main` first. Resolve conflicts. Verify `git diff --stat main..sprint/<name>` does NOT show deletions of migrations 0037-0040 or `tests/vault-flows.mjs`.
2. Check the diff: `git log main..sprint/<name> --stat`. Cite anything weird.
3. Merge: `git checkout main && git merge --no-ff sprint/<name>`. Resolve conflicts; ask user if non-trivial (touches >5 files OR a security-critical path).
4. Verify: `npm run build` must pass. Don't proceed if it fails — root-cause, not workaround.
5. Smoke: `node tests/smoke.mjs` must stay 23/23 green against prod. Don't proceed on a regression.
6. Brief the user on what landed before next merge — one line: "merged X, build OK, smoke 23/23".
7. Do NOT push to origin until ALL 9 are merged + user confirms.

### Migration numbering normalization
Two conventions in play — sequential `00NN_` (Ian's style, canonical for this repo) and unix-timestamp (`1778891249`, `1778891250`, `1778891444`, `1778891445`). Before merging branches that use timestamps, rename to next available `00NN_` slot. Check `ls supabase/migrations/ | tail -5` for current high-water mark. As of 2026-05-16 the high mark is `0041_consent_default_fix.sql`, so new migrations start at `0042`.

---

## Phase B — Apply migrations (30-45 min, USER-GATED)

**5 migrations may exist after merges:**
- `supabase/migrations/<NNNN>_close_schema_drift.sql` (additive, safe)
- `supabase/migrations/<NNNN>_rls_harden.sql` (CRITICAL — closes cross-tenant write leak)
- `supabase/migrations/0037_webhook_replay_seen.sql` (additive, RLS-restricted)
- `supabase/migrations/<NNNN>_recruits.sql` (new feature, additive)
- `supabase/migrations/<NNNN>_user_prefs.sql` (new feature, additive)

**For each migration, in this exact order:**
1. Show the user the migration content + brief summary of blast radius.
2. Ask: "Apply this to production? (y/n)"
3. If yes, run via the project's standard apply path. Check `scripts/` and `package.json` for an existing helper (probably `supabase db push` or a Python script).
4. If the migration fails, STOP. Do not retry. Report error verbatim. Wait for user direction.

The RLS-harden migration MUST go after schema-drift. The dependency is real (RLS policies reference columns added by drift).

**Also flag for separate migration** (from `audits/ONBOARDING_GAPS.md`): 5 RPCs needed by the onboarding wizard don't exist in deployed Supabase. The hard blocker is `provision_sub_agency`. If user wants the onboarding flow to actually work for fresh agencies in this session, you'll need to write a 6th migration containing those RPCs. Ask the user — this is bigger scope than the merge.

---

## Phase C — Production validation (15 min)

After all approved migrations applied:
1. Push merged main to origin: `git push origin main`.
2. Wait for Vercel deploy (~90s). Check via `curl -sI https://repflow.koino.capital | head -5` for fresh `date:` header.
3. Re-run `node tests/smoke.mjs`. Must be 23/23 green.
4. Spot-check the patched flows in browser:
   - Vault → Carriers → "Manage in Admin" deeplink (was broken pre-RLS migration if RLS scope changes affected the carriers query)
   - CRM → Pipeline kanban DnD (drag a card; assert it persists)
   - Floor → autodial pin from a CRM lead → assert the Start button reflects the count
5. Brief the user with explicit citations: commits merged, migrations applied, smoke result, any regression seen.

---

## Phase D — Recruiting funnel real build (90-180 min)

**Goal:** Take the recruiting page from "scaffold or workbench" to a working applicant-intake + funnel-management flow. Per CLAUDE.md, the funnel is **Applied → Discovery Call → Onboarding → Licensed** and the mission is to autonomously run it.

**Read first:**
- `page-recruiting.jsx` (Ian's workbench, `recruiting_applicants` / `_campaigns` / `_messages` tables)
- `page-recruits.jsx` (kanban scaffold from sprint, `recruits` table)
- `audits/SPRINT_RETRO_2026-05-15.md` for the deviation note (both pages coexist after the merge)
- The existing `recruiting_applicants` schema in `supabase/migrations/`

**Decide first:** which page is canonical going forward? Ask the user. Do not make this choice unilaterally — it affects all subsequent work.

**Then ship the missing pieces** (scope down honestly if you run out of time):

1. **Public applicant intake form** (`apply.html` or `/api/recruits/intake` + a form). Lands in the canonical applicants table with `stage='Applied'`. No auth required (public-facing form). Include: name, email, phone, state, NPN if licensed, source. Validate, sanitize, dedupe by email.

2. **Funnel page wires intake source.** Whichever page is canonical, ensure incoming applicants appear there in real time (Supabase subscription on the table).

3. **Stage-transition automations** (minimum viable):
   - Applied → Discovery: SMS the applicant the Koino booking link `https://cal.com/koino` via the existing Twilio helper. Check `api/sms/*` for the helper.
   - Discovery → Onboarding: SMS a onboarding-checklist URL.
   - Onboarding → Licensed: log to `~/koino-self/recruits.log` AND notify Ian. Notification policy: **try Telegram MCP first; if it fails, fall back to SMS to Ian's number** (OCI has it — check `~/koino-self/` configs or the `cmo-*` skills for the canonical number lookup). Try once each, don't loop. Log the channel actually used.
4. **Log every applicant to `~/koino-self/recruits.log`** (CLAUDE.md ground-truth log) on insert and on every stage transition. Format: `YYYY-MM-DDTHH:MM:SSZ | <recruit-id> | <name> | <stage>`.

**Skip if no time:** Telegram bot training nudges, full SMS sequence automation, Calendly OAuth integration (the hardcoded `https://cal.com/koino` link above is fine for v1). Note these in `audits/RECRUITING_FUNNEL_REMAINING.md`.

---

## Rules (non-negotiable, in priority order)

1. **Anti-hallucination.** Every claim cites a file:line or command output. If you can't verify, say "TODO — needs verification" and move on.
2. **No push without explicit user confirmation per push.** "Deploy it", "push it", "ship it" = green light. Anything else = wait.
3. **No `supabase db push` or migration application without per-migration user approval.** Even for additive ones.
4. **If a build or smoke fails, stop and report.** Don't retry, don't work around, don't `--no-verify`.
5. **No new parallel agent sprints from this session.** Sequential work only. We are MERGING, not adding more in-flight branches.
6. **Verify spec assumptions before delegating.** The prior session had 3 wrong spec assumptions agents had to correct. Grep first.
7. **Be terse.** Hormozi-direct. Cite, don't narrate.

---

## Success criteria

- All approved `sprint/*` branches merged to main with build green + smoke green.
- All approved migrations applied to deployed Supabase, RLS leak closed.
- `main` pushed to origin, Vercel serving the new build, smoke 23/23 green post-deploy.
- Recruiting funnel: applicant intake form live OR `audits/RECRUITING_FUNNEL_REMAINING.md` exists with a tight scope of what's left.
- A final retro file `audits/SPRINT_RETRO_<date>.md` capturing what shipped, what didn't, and one-line next-step.

If you hit any cap (time, decision-block, ambiguity), STOP, write the retro, hand back. Don't pretend to finish something you didn't.

Start by running the inventory commands above and reporting what you find. Don't touch anything until the user confirms direction.
