"""fb_pull_lead_forms — pull new leads from Facebook Lead Ads via Graph API."""
from ._stubs import make_stub
REQUIRED_CAPS = ["db.read_own_pipeline"]
run = make_stub("fb_ads", "pull_lead_forms", REQUIRED_CAPS)
